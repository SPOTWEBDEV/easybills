# Base URL for the EasyBills API (see the integration guide you supplied).
# Defaults to the production URL in src/lib/api.ts if this is not set.
NEXT_PUBLIC_API_BASE_URL=https://api.easybills-app.top/api/v1

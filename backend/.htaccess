# If your hosting control panel points the domain at this project's root
# (instead of directly at /public), this redirects everything into public/.
# If you can set the document root to /public directly, you can delete
# this file.
RewriteEngine On
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ /public/$1 [L]
